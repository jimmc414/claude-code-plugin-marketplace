# Composition with  and caveman — full spec Pointer target from `SKILL.md` `## Caveman coexistence`. The load-bearing
independence rule and the compatibility table stay in `SKILL.md` itself
(smoke-gated); this file holds the surrounding rationale. ##  owns public wording On public reader-facing prose surfaces specifically, `` — not
caveman — owns the wording inside nodes and `↪` leaves (see 
`## Composition with the other output layers`). caveman stays out of those
surfaces entirely, so the `SKILL.md` compatibility table applies only where
caveman is the active wording layer (non-public, computer-facing surfaces). ## Compose per node Only when caveman is separately active:
1. place by marker — nested-notes
2. enumerator wording compressed — **caveman, not nested-notes**
3. `↪` leaf text left readable — nested-notes
